# Sales-Summary-from-SQLite-Database-using-Python
Creates a sales table (if it doesn't exist)

Inserts dummy sales data

Uses SQL to aggregate total quantity and revenue per product

Prints the results

Plots a bar chart of revenue by product
